## What and why

<!-- What does this change, and what problem does it solve? Link issues with "Fixes #123". -->

## How it was tested

<!-- Commands run, screenshots for dashboard changes (desktop and ~390 px phone width). -->

## Checklist

- [ ] `uv run ruff check .` and `uv run ruff format --check .` pass
- [ ] `uv run mypy src/` passes
- [ ] `uv run pytest` passes, with new or updated tests for this change
- [ ] Version bumped in `pyproject.toml` and `CHANGELOG.md` updated (user-visible changes)
- [ ] No API keys, `.env` files, databases or personal data included
